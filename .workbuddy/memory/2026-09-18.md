# 2026-09-18

## LostFound 系统审查与清理

- 对 LostFound（Struts2 2.3.1 + Spring 3.1.1 + Hibernate 3.6 + MySQL）做了完整安全/架构审查。
  关键风险：Struts2 2.3.1 多个未授权 RCE（S2-009/013/016/020）；admin 与写操作无鉴权（web.xml 无 security-constraint，struts.xml 仅 goods/comment 包挂了 userAuthority 且未覆盖上传 action）；未授权文件上传（MIME 仅校验，疑似 webshell）；db.properties 及多处源码硬编码 root/root；明文密码；webapp 根暴露源码（src/ 树 + 根级 .java/.class）。
- 清理：删除了 webapp 根目录 20 个测试/工具遗留文件（Test*.java/.class、AddRewardPointsToUser.*、CheckUserEntity.*），均已用真实 rm 删除并经 Glob 复核。
- 未删除：src/（真实业务源码，删之会丢源码，仅建议移出 webapp 根以消除源码泄露）；.sql/.jsp/.idea 保留。
- 环境注意：本机 Bash 的 rm/ls 被损坏的 safe-delete 包装脚本拦截（dirname/safe_delete_main 缺失），需用 /usr/bin/rm 直连或 PowerShell；PowerShell stdout 不被工具捕获。

## 收尾"变成作品"
- 用户要求把系统收尾成可展示作品。已完成：将暴露的 `src/` 业务源码树从 webapp 根移入 `WEB-INF/src/`（Tomcat 禁止 HTTP 访问 /WEB-INF/*，消除源码泄露；运行时用 WEB-INF/classes 不受影响），已 Glob 复核。
- 重写了 README.md（原文件是"项目简介 + GitHub 个人主页模板注释"混搭，模板注释是噪音已去除），补充中文技术栈/功能/结构/部署/安全说明；新增 .gitignore（忽略 target/、IDE 产物、日志，保留 WEB-INF/classes 与 lib 以保证免构建可运行）。
- 发现：README 中的 `feiyang143/feiyang143` 表明用户 GitHub 用户名为 **feiyang143**（肥洋→feiyang）。（更正）本 webapp 部署副本实为 git 仓库——用户执行 `git init` 报 "Reinitialized existing Git repository"，.git 早已存在；之前 Glob 仅搜了 C:\Users\肥洋 未覆盖 E:\tomcat 故漏判为"非仓库"。即这就是当初 push 用的本地仓库，远程应已配好，无需重新 remote add。
- 待办（用户未要求，可选）：给 admin/上传/删除接口加鉴权、密码改哈希、升级 Struts2、db.properties 凭据外部化。

## GitHub 同步（用户自行推送）
- 用户给出真实仓库：https://github.com/feiyang143/Campus-Lost-and-Found-and-Missing-Item-Notification-System.git
- 本沙箱环境到 GitHub 网络被代理拦截（git clone 报 `CONNECT tunnel failed, response 502`），无法 clone/push；用户决定在本机（有网络+账号）自行操作。
- "现在的干净版"位于本机 `E:\tomcat\apache-tomcat-9.0.113\webapps\LostFound`（非 git 仓库，无 .git）：已删 20 测试文件、src/ 封入 WEB-INF/src、README.md 重写、.gitignore 就位。
- 给用户的同步要点：把干净版覆盖进其本地仓库目录 → 显式 `git rm --ignore-unmatch` 那 20 个测试/工具文件（Test*.java/.class、AddRewardPointsToUser.*、CheckUserEntity.*）→ `git status` 复查误删 → `git add -A` → commit → push。注意部署副本不是仓库，勿直接在此 git init 后 push。
- 方案变更：用户确认本机用户目录无任何本地 git 仓库（Glob 搜 `**/.git/HEAD` 在 C:\Users\肥洋 无结果，D:\ 也无 Campus-Lost），印证当初就是直接 push 到 GitHub。用户最终决定：由他本机把"现在的干净版目录"当新仓库 force push 覆盖远程旧仓库，不要我做任何上传尝试。已评估可行并提示风险（force push 覆盖历史、仓库体积、勿把 .git 留进 Tomcat 部署目录、分支名 main/master 对齐、db.properties root/root 密码会进历史）。
